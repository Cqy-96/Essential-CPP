/*
//算术运算符
a + b  //加法
a - b  //减法
a*b  //乘法
a / b  //除法
a%b  //取余

5 / 3 = 1   5 % 3 = 2

const int lin_size = 8;
int cnt = 1;
//以下语句会执行多次，每次执行完，cnt会增加1
cout << a_string << (cnt % lin_size ? " " :  "/n");

expr
? 如果expr为True，执行该语句
	: 如果expr为False，执行该语句

	cnt += 1  //等同于 cnt=cnt+1


	int cnt = 1;
	++cnt;

	a == b; //等于
	a != b; //不等于
	a < b; //小于
	a > b; //大于
	a <= b; //小于等于
	a >= b; //大于等于
	

}
*/